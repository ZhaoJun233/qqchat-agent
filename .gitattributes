# 换行统一：仓库里一律存 LF，Windows 检出也保持 LF（避免 CRLF/LF 混着提交）
* text=auto eol=lf

# 必须保留 CRLF 的（Windows 批处理）
*.bat text eol=crlf
*.cmd text eol=crlf

# shell 脚本必须是 LF，否则在 Linux（含容器/服务器）上会因为 \r 报错
*.sh text eol=lf

# 二进制
*.png binary
*.jpg binary
*.gif binary
*.webp binary
*.zip binary
*.gz binary
